// Buckets: type definitions, instances, field read/write, objects returned from recipes

bucket Order {
    ingredient recipeName = "Original"
    ingredient pieces = 8
    ingredient isSpicy = RAW
}

bucket Meal {
    ingredient content = ""
    ingredient isSpicy = RAW
}

recipe cook withSides: pieces, spicy {
    Meal m
    m.content = "chicken x " + pieces
    m.isSpicy = spicy
    serve m
}

Order myOrder
myOrder.pieces = 12
myOrder.isSpicy = COOKED
orderUp(myOrder.recipeName)     // Original
orderUp(myOrder.pieces)         // 12
orderUp(myOrder.isSpicy)        // COOKED

Meal dinner = cook(myOrder.pieces, myOrder.isSpicy)
orderUp(dinner.content)         // chicken x 12
orderUp(dinner.isSpicy)         // COOKED
